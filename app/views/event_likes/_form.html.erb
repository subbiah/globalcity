<%= form_for(@event_like) do |f| %>
  <% if @event_like.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_like.errors.count, "error") %> prohibited this event_like from being saved:</h2>

      <ul>
      <% @event_like.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
