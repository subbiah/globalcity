<h1>Listing flats</h1>

<table>
  <thead>
    <tr>
      <th>Flat</th>
      <th>Flat types</th>
      <th>Building</th>
      <th>Active flag</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @flats.each do |flat| %>
      <tr>
        <td><%= flat.flat %></td>
        <td><%= flat.flat_types %></td>
        <td><%= flat.building_id %></td>
        <td><%= flat.active_flag %></td>
        <td><%= link_to 'Show', flat %></td>
        <td><%= link_to 'Edit', edit_flat_path(flat) %></td>
        <td><%= link_to 'Destroy', flat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Flat', new_flat_path %>
