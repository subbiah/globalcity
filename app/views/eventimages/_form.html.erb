<%= form_for(@eventimage) do |f| %>
  <% if @eventimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eventimage.errors.count, "error") %> prohibited this eventimage from being saved:</h2>

      <ul>
      <% @eventimage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_url %><br>
    <%= f.text_field :image_url %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br>
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :image_type %><br>
    <%= f.text_field :image_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
